spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:econdashboard}
    username: ${DB_USERNAME:econdashboard}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env,loggers,conditions,configprops
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.econdashboard: DEBUG
